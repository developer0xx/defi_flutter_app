import 'package:flutter/cupertino.dart';

class AppValues{
  static final smallBorderRadius = BorderRadius.circular(10);
  static final bigBorderRadius = BorderRadius.circular(20);
  static final screenPadding = EdgeInsets.symmetric(horizontal: 30, vertical: 20);
}